# ---------- .NET ----------
[Bb]in/
[Oo]bj/
*.user
.vs/

# SQLite database files (local dev data)
*.db
*.db-shm
*.db-wal

# ---------- Node / frontend ----------
node_modules/
dist/
dist-ssr/
*.local

# ---------- Editor / OS ----------
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# ---------- Secrets / env ----------
.env
.env.*
!.env.example
appsettings.*.local.json
# Environment-specific config can hold real secrets (Stripe keys, etc.) — keep it local.
# Real secrets belong in `dotnet user-secrets`, never in the repo.
appsettings.Development.json
appsettings.Production.json
